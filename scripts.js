function jumlahVolumeKubus(a, b) {
    var volumeA;
    var volumeB;
  
    var totalVolume;

    volumeA = a * a * a;
    volumeB = b * b * b;

    total = volumeA + volumeB;

    return total
}
alert(jumlahVolumeKubus(8, 3));
